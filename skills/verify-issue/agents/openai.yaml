interface:
  display_name: "Verify Change"
  short_description: "Verify a change in a fresh context"
  default_prompt: "Use $verify-issue in a fresh task to verify this change against its accepted specification."
