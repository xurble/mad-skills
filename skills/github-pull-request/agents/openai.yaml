interface:
  display_name: "GitHub Pull Request"
  short_description: "Perform the requested PR action"
  default_prompt: "Use $github-pull-request for only the explicitly requested PR actions and stop afterward."
